services:
  using.run:
    container_name: using.run
    hostname: using.run
    build:
      context: ..
      dockerfile: ./docker/build.dockerfile
      args:
        - PORT=3000
    expose:
      - 3000
    environment:
      PORT: 3000
    restart: unless-stopped
